export default interface Theme {
  theme: {
    color: {
      shadowLight: string;
      background: string;
      primary: string;
      secondary: string;
      secondaryAlternative: string;
      text: string;
      contextMenu: string;
    };
  };
}
